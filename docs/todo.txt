-Add GetID3 plugin to site and have it get metadata on image and video files
-Add MediaElement.js media player to site to put in Modal (or maybe just use something simpler)
-Add "stream" capability to site (using Alberto's script)
-Only accept certain file types (mp4, wmv, flv, mp3)
